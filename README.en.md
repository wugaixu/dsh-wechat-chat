# Whale Chat (dsh-wechat-chat + Android app)

[English](README.en.md) | [中文](README.md)

> Repo: <https://github.com/wugaixu/dsh-wechat-chat> · Version **1.1** · MIT License

Turn your PC's DeepSeek Harness Web into a "WeChat-style chat": install the
Android app "鲸聊" (Whale Chat), scan a QR code to pair, then text the agent on
your PC just like WeChat. Messages land in a **real session** on the PC's Web UI
(visible in the sidebar); replies are generated by the PC's normal API calls and
pushed back to the phone.

## How it works

The plugin ships a **complete, self-contained connection scheme** (the same
mechanism ported from `@linxin666/dsh-remote-web-ui`) — no dependency on the
"family bundle" plugins (remote access & desktop launcher are disabled by id):

```
Android app (WebView WeChat-style page /wechat)
   │  scan: QR encodes <base>/wechat?pair=<one-time token>
   │        /wechat validates the token → sets device cookie whale_pair (1y) → chat
   ▼
paired device (cookie / header / query) → this plugin's gated /api/wechat/* routes
   │
   ├─ state     session state + history + avatar/nickname
   ├─ send      submit text as a user message into the PC's real session
   ├─ events    polled push: thinking / using tool / final answer / error
   └─ cancel    stop the current turn
```

- Pairing entry on the PC: the whale icon at the bottom of the official sidebar
  (next to Settings), or open `http://127.0.0.1:3080/whale-panel` (loopback only)
  to show the QR panel. The QR points at a **free public tunnel** by default
  (Cloudflare quick tunnel — no account, no cost, `cloudflared` shipped with the
  plugin); falls back to the first LAN address when the tunnel is not ready, or a
  configured `publicBaseUrl` if you have your own tunnel.
- Security matches the original remote plugin: one-time tokens (10 min), device
  sessions persisted to `$DSH_HOME/whale-devices.json` (30-day idle expiry, max 4,
  FIFO eviction), pairing control endpoints loopback-only, and non-loopback chat
  endpoints require a live device credential.
- The phone implements **none** of the harness chat protocol — it is just a remote control.
- One persistent session per paired device (mapping in
  `$DSH_HOME/wechat-chat-devices.json`), titled "鲸聊 · 手机" by default, visible
  in the PC sidebar.

## File layout

```
dsh-wechat-chat/
├─ plugin/                       # DSH cordis host plugin (installable standalone)
│  ├─ package.json
│  ├─ verify-release.mjs         # release self-check: npm pack + clean install + import host module
│  └─ lib/
│     ├─ index.js                # routes + session driver + pairing/gating/tunnel + poll push
│     ├─ chat-page.html          # WeChat-style chat page (single file)
│     ├─ panel-page.html         # PC pairing panel (QR)
│     ├─ client.js               # client half (sidebar footer entry)
│     └─ qrcode.js               # bundled qrcode-generator (MIT)
├─ app/                          # Android project (WebView shell + ZXing + UCrop + system STT)
│  └─ app/src/main/...           # MainActivity.java / layouts / firstrun.html / icons / manifest
├─ sdk-fetch.mjs                 # fetch Android SDK packages manually
├─ toolchain-setup.ps1           # toolchain extract/install script
├─ 鲸聊-v1.1.apk                 # prebuilt APK (install directly on the phone)
├─ README.md / README.en.md / LICENSE / .gitignore
```

## Install

### Install the plugin

**Option A — from GitHub (no npm publish needed)**

```bash
dsh plugin --profile web add github:wugaixu/dsh-wechat-chat
```

**Option B — local directory**

Copy the `plugin/` directory to
`$DSH_HOME/profiles/web/user-patches/dsh-wechat-chat/`, add
`"dsh-wechat-chat": "file:./user-patches/dsh-wechat-chat"` to
`profiles/web/package.json`, and register it in
`profiles/web/cordis.patch.yml`:

```yaml
- insert:
    - id: wechat-chat
      name: dsh-wechat-chat
      config:
        autoTunnel: true
        model: deepseek-v4-flash
        reasoningEffort: low
```

> ⚠️ Same lineage as `@linxin666/dsh-remote-web-ui`: this plugin bundles its own
> scan-pairing + gating + public tunnel. If you also run that family-bundle
> plugin, disable `web-ui-remote-web-ui` and `web-ui-desktop-launcher` in
> `cordis.patch.yml` first to avoid feature/port collisions.

Restart, then verify:

```
C:\Users\Administrator\.dsh\launcher\stop-dsh-web.ps1
C:\Users\Administrator\.dsh\launcher\start-dsh-web.cmd   (or restart from the tray)
```

- `http://127.0.0.1:3080/whale-panel` shows the pairing QR panel;
- `http://127.0.0.1:3080/wechat` shows the WeChat-style chat page.

### Build / install the APK

1. **Build** (JDK 17 + Gradle 8.14.2 + Android SDK; point `sdk.dir` in
   `local.properties` at your SDK):
   ```powershell
   gradle.bat -p app assembleDebug --no-daemon
   ```
   Output: `app/app/build/outputs/apk/debug/app-debug.apk`.
2. **Install**: copy the prebuilt `鲸聊-v1.1.apk` to the phone (allow unknown
   sources), or `adb install 鲸聊-v1.1.apk`.
3. **Use**: open the pairing panel on the PC → in the app tap "扫一扫连接" to scan →
   auto-pair into the chat. Subsequent launches go straight to the chat.

## Configuration

Plugin `config` in `cordis.patch.yml` (all optional):

| Key | Default | Meaning |
| --- | --- | --- |
| `nickname` | `鲸聊助手` | nickname shown atop the phone chat page |
| `title` | `鲸聊 · 手机` | session title in the PC sidebar |
| `provider` | `deepseek-official` | model provider |
| `model` | `deepseek-v4-flash` | model |
| `reasoningEffort` | `low` | reasoning effort |
| `autoTunnel` | `true` | auto-start a free Cloudflare quick tunnel |
| `publicBaseUrl` | none | your own public base (takes priority over tunnel) |
| `tokenTtlMs` | `600000` | pairing token validity (ms) |
| `idleExpireMs` | `2592000000` | device idle expiry (30 days) |
| `maxDevices` | `4` | max paired devices |

**Avatar / background**: tap the avatar in the app to change it (or drop images
into `$DSH_HOME/wechat-chat/avatars/`: `other.*` assistant avatar, `me.*` your
avatar, `background.*` chat background).
**Styling**: edit the CSS in `plugin/lib/chat-page.html` (refresh the page, no restart).

## Develop & release

```bash
cd plugin
npm run verify    # release self-check: syntax check + clean tarball install + import host module
```

`verify-release.mjs` runs `npm pack` against the `files` whitelist, installs the
tarball into a clean temp consumer, and imports the host module — proving the
publish form resolves itself like the DSH loader would (this plugin has **zero
runtime dependencies**: only Node built-ins + `ctx` services).

**Publishing to GitHub**: this repo is the release source. After editing,
`git add -A && git commit && git push`. The `files` whitelist and `.gitignore`
already exclude `node_modules`, `*.tgz`, `package-lock.json`, Gradle build
outputs, `local.properties`, and runtime user data (`avatars/`); the prebuilt
`鲸聊-v1.1.apk` is kept in the repo.

## Known limitations

- Text only: the phone sends/receives plain text; code blocks / markdown render as plain text.
- One at a time: sending while a turn is running is rejected (409); the top-right
  status shows progress. The cancel button is not yet exposed in the UI
  (the `/api/wechat/cancel` endpoint exists).
- The free tunnel changes address on every dsh-web restart (trycloudflare quick
  tunnel behavior): re-scan the QR once after a restart (scanning updates the
  server address and re-pairs); LAN use is unaffected.
- Plain-HTTP LAN install/use works (the app allows cleartext traffic).

## Security

A paired device is a full-control credential (same model as the existing remote
plugin). Stop/revoke pairing immediately cuts the channel; the chat page and API
become unavailable. The four control surfaces (pairing, self-update, plugin
management, desktop launcher) are never reachable from a remote device.
